// frontend/src/api/client.ts
// Bridge file: align legacy imports with new lib client.
// Components importing `api` or `apiFetch` from here will work.
export * from '../lib/client';
export { default } from '../lib/client';
