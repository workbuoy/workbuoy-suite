const { planAndExecute } = require('./../ai/buoy/plan_and_call');const base=process.env.API_BASE_URL||'http://localhost:3000';const key=process.env.API_KEY||'changeme';(async()=>{try{const intent={method:'GET',path:'/api/v1/crm/contacts'};const res=await planAndExecute(intent,{baseUrl:base,apiKey:key});process.exit(res.status>=200&&res.status<300?0:1)}catch(e){console.error('AI smoke error:',e.message);process.exit(2)}})();
