import { undoAction } from '../../../../lib/ai/actions.js';import { resolveTenantId } from '../../../../lib/middleware/tenant.js';export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'method_not_allowed'});const tenant_id=resolveTenantId(req);const {action_id}=req.body||{};const out=await undoAction(tenant_id,action_id);res.json(out);}