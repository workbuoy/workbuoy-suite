name: sdk-py-publish

on:
  push:
    tags:
      - 'sdk-py-v*'

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Derive version from tag
        id: v
        run: |
          TAG="${GITHUB_REF_NAME}"
          VERSION="${TAG#sdk-py-v}"
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
      - name: Set version in pyproject
        run: |
          sed -i "s/^version = ".*"/version = "${{ steps.v.outputs.version }}"/" sdk/python/pyproject.toml
      - name: Build
        run: |
          python -m pip install --upgrade build
          python -m build
        working-directory: sdk/python
      - name: Checksums
        run: |
          cd sdk/python/dist
          for f in *; do sha256sum "$f" >> SHA256SUMS.txt; done
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: pypi-dist
          path: sdk/python/dist/**
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: sdk/python/dist
        env:
          PYPI_API_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
      # - name: (Optional) Sigstore attest
      #   uses: sigstore/gh-action-sigstore-python@v2
      #   with:
      #     inputs: "sdk/python/dist/*.whl sdk/python/dist/*.tar.gz"
